---
const {data} = Astro.props
---
<article>
    <a href={`/speaker/${data.slug}`}>
	<img src={`/product_pic/${data.image}`} alt="Produkt">
    <div>
	<h3>{data.name}</h3>
	<p class="burning">{data.burning}</p>
	<p class="price">{data.price}</p>
    </a>
    </div>
</article>